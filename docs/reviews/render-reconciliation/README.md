# Reconciliation validation

Current validation output for the bounded renderer/refit reconciliation based on `c9429e0`. Historical Package A evidence is retained separately in `../package-a`.

Native resource/recovery and shader smoke checks do not certify art direction or performance budgets. Full status is recorded in `../../RENDER-RECONCILIATION.md`.
